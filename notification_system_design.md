Notification System Design

There were too much notifications so user was missing many. 

So I prioroitized it with score system each notification gets a score based on (type weight=  Placement as 3, Result as 2, event as 1). 

formula = (weight * 0.7) +(recency* 0.3)

after each notification sorting is done
